import styled from 'styled-components';

export const BackArrow = styled.i`
    position: fixed;
    color: black;
    top: 20px;
    left: 40px;
    font-size: 30px;
    transition: ease-in-out 0.3s;

&:hover {
    color: #FF8985;
    transform: scale(1.1);
}
`
